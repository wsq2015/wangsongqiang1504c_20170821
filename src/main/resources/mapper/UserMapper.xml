<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bawei.mapper.UserMapper">
    <insert id="useradd" parameterType="com.bawei.bean.t_user">
        INSERT INTO t_user(names,username,password) values(#{names},#{username},#{password})
    </insert>
  <!--<select id="login" resultType="com.bawei.bean.t_user">
        SELECT  * FROM t_user WHERE username=#{username} AND  password = #{password}
    </select>-->

</mapper>